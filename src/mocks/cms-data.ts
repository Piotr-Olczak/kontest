import { delay } from 'mocks/mocks';
import { cmsDataHowToPlay, cmsDataJSONraces } from 'mocks/cms-data/cms-data';

export const mockCmsDataHowToPlay = () => delay(cmsDataHowToPlay);
export const mockCmsDataRaces = () => delay(cmsDataJSONraces);
